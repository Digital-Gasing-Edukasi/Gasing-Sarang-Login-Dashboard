name: Deploy to Staging

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: sacred-octagon
  ZONE: asia-southeast2-a
  # Nama instance GCE untuk staging (akan diisi nanti di GitHub Secrets)
  GCE_INSTANCE: ${{ secrets.GCE_INSTANCE_STAGING }}
  # Direktori tujuan di server (misalnya untuk Nginx)
  TARGET_DIR: /var/www/staging.gasingcircle.com

jobs:
  build-and-deploy:
    name: Build and Deploy (Staging)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Application
        run: npm run build

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          # Sangat disarankan menggunakan Workload Identity Federation, 
          # namun Service Account Key JSON juga bisa digunakan (lebih mudah untuk awal).
          credentials_json: '${{ secrets.GCP_CREDENTIALS_STAGING }}'

      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Transfer Build to GCE Staging
        # Mentransfer folder dist ke VM Staging menggunakan gcloud scp
        run: |
          gcloud compute ssh ${{ secrets.GCE_USER }}@${{ env.GCE_INSTANCE }} \
            --zone ${{ env.ZONE }} \
            --project ${{ env.PROJECT_ID }} \
            --command "sudo mkdir -p ${{ env.TARGET_DIR }} && sudo chown -R ${{ secrets.GCE_USER }}:${{ secrets.GCE_USER }} ${{ env.TARGET_DIR }}"
          
          gcloud compute scp --recurse dist/* ${{ secrets.GCE_USER }}@${{ env.GCE_INSTANCE }}:${{ env.TARGET_DIR }} \
            --zone ${{ env.ZONE }} \
            --project ${{ env.PROJECT_ID }}
